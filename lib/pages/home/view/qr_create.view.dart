import 'package:flutter/material.dart';

class QrCreateView extends StatelessWidget {
  const QrCreateView({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(child: Text('foo'));
  }
}
